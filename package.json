{
  "name": "cetin-design-site",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init:remote": "wrangler d1 execute cetin-design-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
